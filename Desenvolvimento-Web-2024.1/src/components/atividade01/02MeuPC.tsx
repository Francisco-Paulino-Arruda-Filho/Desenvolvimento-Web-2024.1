const PlacaMae = (nome: string, preco: number) => {
    return (
        <><h1>{nome}</h1><h1>{preco}</h1></>
    )
};

const Memoria = (nome: string, preco: number) => {
    return (
        <><h1>{nome}</h1><h1>{preco}</h1></>
    )
};

const PlacaDeVideo = (nome: string, preco: number) => {
    return (
        <><h1>{nome}</h1><h1>{preco}</h1></>
    )
};  

export {PlacaDeVideo, PlacaMae, Memoria}